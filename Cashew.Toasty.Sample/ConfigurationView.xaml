<UserControl x:Class="Cashew.Toasty.Sample.ConfigurationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:sample="clr-namespace:Cashew.Toasty.Sample"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <sample:SystemConfiguration Grid.Column="0" Margin="5"/>

        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <StackPanel Grid.Row="0">
                <Label Content="Type:"/>
                <ComboBox x:Name="Type" SelectionChanged="Type_OnSelectionChanged" Margin="5"/>
            </StackPanel>

            <Grid Grid.Row="1" x:Name="ConfigGrid">
                <sample:ToastConfigView x:Name="ToastConfig" Margin="5" Grid.Row="1"/>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
